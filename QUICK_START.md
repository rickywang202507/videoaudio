# 快速开始指南 - TextNow 反机器人检测

## 🚀 快速开始（3步）

### 第1步：安装依赖

运行安装脚本：

```bash
setup_stealth.bat
```

或手动安装：

```bash
pip install selenium-stealth
pip install -r requirements.txt
```

### 第2步：启动Stealth模式Chrome

```bash
run_chrome_stealth.bat
```

**重要**：
- ✅ 在打开的Chrome窗口中**手动登录TextNow**
- ✅ 完成任何验证码
- ✅ **保持窗口打开**（可以最小化）

### 第3步：运行自动化

```bash
python textnow_automation.py
```

或使用批处理：

```bash
run_monitor.bat
```

## ✅ 验证效果

运行测试脚本：

```bash
python test_anti_detection.py
```

这会测试你的配置是否能绕过机器人检测。

## 📋 文件说明

| 文件 | 用途 |
|------|------|
| `setup_stealth.bat` | 一键安装所有依赖 |
| `run_chrome_stealth.bat` | 启动增强的Chrome（**必须先运行**） |
| `textnow_automation.py` | 主自动化脚本（已更新） |
| `test_anti_detection.py` | 测试反检测效果 |
| `stealth_injector.js` | JavaScript反检测代码 |
| `stealth_utils.py` | Python工具库 |
| `ANTI_BOT_SOLUTION.md` | 详细文档 |

## ⚠️ 常见问题

### 问题1：仍然被检测为机器人

**解决**：
1. 确保使用 `run_chrome_stealth.bat`（不是旧的debug版本）
2. 检查 `selenium-stealth` 是否安装：
   ```bash
   pip install selenium-stealth
   ```
3. 在Chrome中手动完成一次完整操作
4. 增加操作间隔（修改第750行的 `wait_sec`）

### 问题2：无法连接到Chrome

**解决**：
1. 先运行 `run_chrome_stealth.bat`
2. 确保Chrome窗口保持打开
3. 检查端口9222是否被占用

### 问题3：验证码无法通过

**解决**：
1. 在 `run_chrome_stealth.bat` 打开的窗口中**手动完成**验证码
2. 不要关闭窗口
3. 验证通过后，脚本会继续使用这个会话

## 🎯 工作原理

### 检测方法
TextNow检测机器人的方式：
- ❌ `navigator.webdriver` 属性
- ❌ Chrome DevTools特征
- ❌ 缺失的浏览器对象
- ❌ 不自然的操作速度
- ❌ 异常的浏览器指纹

### 我们的对策
- ✅ JavaScript注入移除webdriver属性
- ✅ Chrome启动参数隐藏自动化特征
- ✅ 伪装完整的浏览器对象
- ✅ 随机延迟模拟人类行为
- ✅ 正常化浏览器指纹

## 📊 测试结果示例

运行 `test_anti_detection.py` 后的理想结果：

```
✅ Sannysoft Bot Detector: PASSED
✅ BrowserLeaks WebDriver: PASSED
✅ Are You Headless: PASSED

Total: 3 tests
Passed: 3 ✅
Failed: 0 ❌

🎉 Excellent! All tests passed!
```

## 🔧 高级配置

### 增加人类行为模拟

在 `textnow_automation.py` 中调整延迟：

```python
# 第622行 - 点击前思考时间
time.sleep(random.uniform(2.0, 5.0))  # 改为 (3.0, 8.0)

# 第667行 - 发送前校对时间
time.sleep(random.uniform(3.0, 7.0))  # 改为 (5.0, 10.0)
```

### 降低扫描频率

```python
# 第750行 - 扫描间隔
wait_sec = 30  # 改为 60 或更长
```

### 使用代理（可选）

如果IP被标记，在 `textnow_automation.py` 第117行后添加：

```python
self.options.add_argument('--proxy-server=http://your-proxy:port')
```

## 📚 更多信息

详细文档：`ANTI_BOT_SOLUTION.md`

## 🆘 需要帮助？

如果上述方法仍然不够，可以尝试：

1. **undetected-chromedriver** - 更强大的反检测库
2. **浏览器扩展** - 安装真实扩展增加真实性
3. **Canvas/WebGL指纹伪装** - 更深层的指纹修改
4. **专用代理** - 使用住宅IP代理

---

**最后更新**: 2026-01-30
**版本**: 2.0 (Enhanced Stealth)
